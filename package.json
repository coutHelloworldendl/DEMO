{
  "name": "image-captioning-demo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "backend": "cd backend && uvicorn main:app --reload --port 8000",
    "frontend": "npm run dev --workspace=frontend"
  }
}
